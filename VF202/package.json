{
  "name": "VF202",
  "version": "0.0.1",
  "description": "VF202门禁标品",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm i && npx esbuild index.js --bundle --platform=neutral --external:tjs:path --external:tjs:sqlite --external:tjs:ffi --define:process.env.NODE_ENV='\"production\"' --outfile=dist/index.js && cp -r resource dist/resource"
  },
  "author": "dxl",
  "dependencies": {
    "dxDriver": "file:../dxDriver",
    "dxAccess": "file:../dxAccess",
    "dxLib": "file:../dxLib"
  }
}